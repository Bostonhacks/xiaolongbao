export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum Status {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    WAITLISTED = 'WAITLISTED',
    REJECTED = 'REJECTED'
}

export enum AuthProvider {
    EMAIL = 'EMAIL',
    FACEBOOK = 'FACEBOOK',
    GOOGLE = 'GOOGLE'   
}